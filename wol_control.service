[Unit]
Description=Gunicorn service of wol_control
After=network.target

[Service]
User=lastc
Group=www-data
WorkingDirectory=/home/lastc/wol_control
Environment="PATH=/home/lastc/.flask_env/bin:/usr/bin"
ExecStart=/home/lastc/.flask_env/bin/gunicorn --workers 3 --access-logfile wol_control.log --bind unix:wol_control.sock -m 007 wsgi

[Install]
WantedBy=multi-user.target
